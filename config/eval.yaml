experiment:
  name: initial test eval

data:
  root: raw_data/chen_trajectories
  test_subdir: test
  input_length: 2000
  target_length: 250
  step: 1
  decimation: 10

decimation: 10

model:
  checkpoint_path: artifacts/MLP_windows_basic-20251111-081848-6acd350d/checkpoints/epochepoch=01-val_lossval_loss=0.4010.ckpt
output:
  metrics_path: artifacts/eval/metrics.json
  forecast_dir: artifacts/eval/forecasts
  plot_show: True